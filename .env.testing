# Test environment overrides: no RabbitMQ, no Reverb, use test database
QUEUE_CONNECTION=sync
BROADCAST_CONNECTION=log
# Use separate test database so main DB is never reset by tests
DB_DATABASE=notification_dispatcher_test
