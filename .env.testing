# Test environment overrides: no RabbitMQ, no Reverb, use SQLite in-memory
QUEUE_CONNECTION=sync
BROADCAST_CONNECTION=log
# Force SQLite in-memory for tests (even in Docker) to avoid MySQL transaction issues
DB_CONNECTION=sqlite
DB_DATABASE=:memory:
